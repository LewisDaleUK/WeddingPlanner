{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "start": "lerna run --stream --scope core start"
  },
  "workspaces": [
    "packages/*"
  ]
}
